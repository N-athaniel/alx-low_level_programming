#include "main.h"

/**
 * _abs - Computers the absolute value of of an inteeger
 * @t: the integer to be computed
 * Return: the absolute value
 */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
